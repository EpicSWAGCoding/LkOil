import { CardsFilter, Container, Header } from "@/components/shared";

export default async function CardPage() {
    return (
        <Container>
            <Header />
            <CardsFilter />
        </Container>
    )
}